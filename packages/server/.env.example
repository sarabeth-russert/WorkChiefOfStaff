# Server Configuration
PORT=5554
NODE_ENV=development
DATA_DIR=./data
LOG_LEVEL=info

# AWS Bedrock Configuration
# Option 1: Use AWS Profile (recommended for local development)
AWS_PROFILE=default
AWS_REGION=us-east-1

# Option 2: Use explicit AWS credentials (not recommended - use IAM roles or profiles instead)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1

# Option 3: IAM Role (automatically used on EC2/ECS/Lambda - no config needed)

# Bedrock Model Configuration
AWS_BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# Note: AWS credentials will be automatically discovered in this order:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. AWS credentials file (~/.aws/credentials) with AWS_PROFILE
# 3. IAM role (when running on AWS infrastructure)
# 4. Local Bedrock configuration
